# 미니 퀴즈 게임 (mini_quiz_kr.py)

## 소개
세계 각국의 수도를 맞추는 4지선다 콘솔 퀴즈 게임입니다. 1~4번 프로젝트(계산기, 단위 변환기, 숫자야구, BMI 계산기)에서 익힌 내용을 종합한 1단계 마지막 프로젝트입니다.

## 기능
- 문제은행(리스트+딕셔너리 구조)에 저장된 문제를 랜덤한 순서로 출제
- 사용자 입력을 받아 정답 여부 판별
- 오답 시 정답을 안내
- 숫자가 아니거나 범위를 벗어난 입력 시 재입력 요청
- 전체 문제 풀이 후 최종 점수 및 등급 출력

## 사용법
```
python mini_quiz_kr.py
```
문제마다 보기 번호(1~4)를 입력해 정답을 고르면 됩니다. 모든 문제를 풀면 총 정답 개수와 등급이 출력됩니다.

## 주요 함수
- `ask_question(question_data, number)` — 문제 하나를 출력하고, 사용자 입력을 받아 정답 여부(True/False)를 반환합니다. 숫자가 아니거나 범위를 벗어난 입력은 유효한 값이 들어올 때까지 반복해서 재입력을 받습니다.
- `print_grade(score)` — 최종 점수(5점 만점)에 따라 등급 메시지를 출력합니다.
- `run_quiz(question_list)` — 문제은행을 랜덤한 순서로 출제하고, 최종 점수와 등급을 출력합니다.

## 배운 점 / 메모
- `random.shuffle()`은 원본 리스트를 직접 섞기 때문에, `question_list[:]`로 복사본을 만들어 섞어야 원본 데이터가 보존된다.
- `if score = 5:`처럼 `=`(대입)와 `==`(비교)를 혼동하면 `SyntaxError`가 발생한다.
- `try/except`는 실제로 예외를 일으킬 수 있는 코드(`int(user_input)`)만 감싸야 하고, 숫자 범위를 벗어난 값처럼 예외가 나지 않는 경우는 별도의 조건문으로 검증해야 한다.
- 반복되는 값(`len(question_data["choices"])`)은 변수로 한 번만 계산해두면 가독성이 좋아진다.

## TODO / 다음 프로젝트 메모
- 7번 프로젝트(비밀번호 생성기)에서 `secrets` 모듈을 본격적으로 다룰 예정