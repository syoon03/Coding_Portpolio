# 숫자야구 게임
Python으로 만든 콘솔 기반 숫자야구 게임입니다.

## 기능
- 3자리 중복 없는 정답 숫자 무작위 생성
- 사용자 입력에 대한 스트라이크/볼 판정
- 잘못된 입력(숫자 아님, 자릿수 오류, 중복 숫자) 예외 처리
- 정답을 맞힐 때까지 반복 진행 및 시도 횟수 출력

## 주요 함수
- `generate_answer()`: random.shuffle로 중복 없는 3자리 정답 생성
- `judge()`: 정답과 추측을 비교해 스트라이크/볼 개수 반환
- `get_valid_guess()`: 조건을 만족하는 입력이 나올 때까지 반복 검증
- `play_game()`: 전체 게임 진행 흐름 관리

## 메모
- 현재는 `random` 모듈로 정답 생성
- `random`과 `secrets`의 차이는 가볍게 리서치만 해둔 상태
- `secrets`의 실제 적용은 7번 프로젝트(비밀번호 생성기)에서 다룰 예정